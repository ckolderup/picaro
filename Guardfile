guard 'coffeescript', :output => 'public/js', :bare => true do
  watch(%r{app/player/(.+\.coffee)})
  watch(%r{app/editor/(.+\.coffee)})
  watch(%r{app/cms/(.+\.coffee)})

  # Tests
  watch(%r{spec/coffeescript/(.+\.coffee)})
end
